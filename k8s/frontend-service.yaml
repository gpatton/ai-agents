apiVersion: v1
kind: Service
metadata:
  name: agentforge-frontend
  namespace: agentforge
spec:
  type: ClusterIP
  selector:
    app: agentforge-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
